[frontend]
${frontend_ip}

[backends]
%{ for ip in backend_ips ~}
${ip}
%{ endfor ~}

[all:vars]
ansible_user=ec2-user
ansible_ssh_private_key_file=${private_key_path}
ansible_ssh_common_args='-o StrictHostKeyChecking=no'
# Add these lines:
backend1_private_ip=${backend_ips[0]}
backend2_private_ip=${backend_ips[1]}
backup_backend_private_ip=${backend_ips[2]}